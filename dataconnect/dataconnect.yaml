specVersion: "v1"
serviceId: "mardom"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mardom-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
